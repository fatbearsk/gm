## 2026-08-23 - Cordis paper fidelity: extended calculus, confinement, deadlock fix, doc corrections

**Audited gm/rs-plugkit's Cordis-calculus mapping against the actual paper text (not a summary of it) and fixed a real misattribution.** AGENTS.md cited `registry.rs`'s `get_active_provider` as mirroring "Definition 46 (`provider_k(gamma)`)" -- exhaustive extraction of the paper's PDF confirmed `provider_k(gamma)` is defined inline under Definition 45 (the Registry), and Definition 46 is instead the target view `target_n(gamma)` and quiescence predicate `quiet(gamma)`. Corrected in AGENTS.md.

**Added `ExtendedRegistry`/`ExtendedFiber` to `rs-plugkit/calculus.rs`, modeling the paper's Section 4.3 extended ten-rule, four-state calculus** (`Inactive`/`Reloading`/`Active`/`Unloading`, Definition 49) alongside the existing base two-state model -- `relied_n` (Definition 50) as the withdrawal-ordering guard the two-state model has no room for, plus L-Begin/L-Iter/L-Finish/L-Divert/L-Raise/L-Leave/L-Unload.

**Two rounds of independent adversarial subagent review found and this session fixed a real deadlock**: the extended model's `insert` admitted a mutual-requires cycle between two fibers that could permanently stick both in `Unloading` under the `relied_n` guard -- the paper's own Theorem 66/73 are proved only on the explicit (rule-unenforced) hypothesis that the precedence relation (Definition 65) stays acyclic. A first attempted fix declared the check but never threaded the new fiber's `requires` set into it, a no-op; the real fix was live-witnessed via a standalone `rustc` compile of the identical cycle-detection logic against the exact failing case before commit.

**Added a Confinement (Definition 48) self-declared-identity check to `kv_get`/`kv_put`**, then corrected its doc comment after review found it trivially bypassable (gm's dispatch ABI has no caller identity a caller cannot forge) -- documents this honestly as a non-adversarial check, not a security boundary, rather than overclaiming enforcement.

**Explicitly scoped out** Definitions 1-21 (effect independence, transformation monoids) and 33-41 (observational equivalence, two distinct relations) with a stated rationale: both are about the semantics of concrete effect functions this abstract lifecycle model elides throughout, so porting them against an elided `e` would produce vacuous restatements rather than a genuine check.

**Fixed a real config-drift defect found along the way**: `gm-config`'s vendored `fsm/graph.json` was missing the `split-context-swept` gate on `DECIDE->COMPLETE` that rs-plugkit's compiled-default FSM already had, weakening the adversarial-review requirement for multi-file commits project-wide.

## 2026-08-06 - opt-in TencentDB-Agent-Memory-compatible backend for memorize/recall

**gm's memory verbs now have a second, opt-in storage backend.** `memorize`/`recall`/`memorize-fire`/`memorize-prune` continue to default to the existing `.gm/memories/*.md` + `rssearch_vectors` path unchanged; a project can opt a namespace into `memory.tencentdb_backend` in `gm.config.json` (disabled by default, zero behavior change otherwise), routing that namespace's writes through a new file-pointer-only backend (`rs-plugkit`'s `tencentdb_memory` module): content lives as a raw file on disk, the index row holds only a path pointer plus the embedding, never inline text -- unlike the default path's `rssearch_vectors` table, which duplicates text for query convenience. Embedding dimension is independently configurable (default 768, matching TencentDB Agent Memory's local `embeddinggemma-300m` provider), since it never shares a vector column with gm's own fixed 384-dim embedder.

**A separate compat module reads real TencentDB `vectors.db` files for migration/interop.** `rs-plugkit`'s `tencentdb_compat` module targets TencentDB Agent Memory's exact on-disk schema (`l1_records`/`l0_conversations`/`skills`/`embedding_meta`, matching `MemoryCore/src/core/store/sqlite.ts` column-for-column) via a generated Node script run through the already-proven `host_exec_js` mechanism -- confirmed via research this session that no Rust/wasm32-wasip1 path exists for `rusqlite`/`sqlite-vec` (no documented C-compile support for that target), and gm's own libsql-based vector store uses an incompatible native vector format anyway.

**A migration script converts gm's existing memory corpus.** `scripts/migrate-memory-to-tencentdb.mjs` reads `.gm/memories/*.md`, discards content that fails the same `is_derivable_state` check `memorize-fire` already applies at write time (git-log-derivable facts, dated audit entries), and dispatches gm's own `memorize` verb to write survivors through the new backend -- idempotent, `--dry-run` supported. Live-witnessed against gm's own real 892-file corpus (891 kept, 1 correctly discarded) and against a live-enabled project end-to-end (write, recall, prune all verified against the deployed daemon, including a real non-default-dimension ANN query, a code path never previously exercised).

**Found and fixed a real concurrency bug along the way.** `agentplug`'s `host_fs_write` (the actual file-write primitive every guest dispatch, including `prd-add`/`prd-resolve`, funnels through) had no lock and no atomicity -- two concurrent writers to the same path (the shared 4-slot plugin pool dispatches guest writes from independent OS threads) could interleave into a lost update, invisible to the guest-side optimistic CAS retry since the race happened entirely inside the host's `fs::write` call. This is what caused 5 of 7 `prd-add` rows to silently vanish from `.gm/prd.yml` mid-session despite each logging a successful `prd.added` event. Fixed: `host_fs_write` now serializes per-path via a process-wide lock registry and writes through a temp-file+rename, matching the `atomic_write` pattern `memory_md.rs` already used for `.gm/memories/*.md`.

**Also fixed two stale-documentation bugs in the served prose.** `gm-config`'s `entry.md` still named the retired `.gm/rs-learn.db` as the recall store and referenced `mem-*.json` (the real extension is `.md`) -- both actively served to every gm session via the config-repo resolution chain, both corrected.

## 2026-08-06 - agent-memory skill added

**Added `skills/agent-memory`, replacing informal `learn`-adjacent memory guidance with a real skill.** No live `learn` plugin/skill actually existed in this repo (only a retired `rs-plugkit` verb at `wasm_dispatch/verbs.rs` that already errors and points callers at `memorize`/`recall`/`memorize-prune`); confirmed by fresh grep across `skills/`, `build/`, and every manifest before writing this entry. The new skill wraps `AnEntrypoint/agent-memory` (a published fork of `TencentCloud/TencentDB-Agent-Memory`) -- Chat Memory (L0->L1->L2->L3), a versioned Skill library, Wiki/CodeGraph, and a human-reviewed team memory panel -- for cross-session, cross-framework, cross-team agent memory. It is explicitly scoped apart from this project's own `memorize-fire`/`recall` store; the skill's own description states the distinction so the picker doesn't conflate the two. Registered in `SKILLS.md`. `build/gm-*` host packages are gitignored generated artifacts with no in-repo generator script, so they pick up the new skill on their next external regeneration rather than being hand-edited here.

## 2026-08-04 - paper page TOC hierarchy, mermaid lightbox, header-offset sync landed and live-witnessed

**The paper page TOC had no visual hierarchy across 48 flat entries.** Grouped the "Operational Hard Rules" and "Third-Generation Dispatch-Boundary Fixes" runs into nested `<ol>` blocks under a `.toc-group` label in `docs/paper.html`, styled via new rules in the design SDK's `gm-prose.css`. Live-witnessed: 2 groups render on the deployed page.

**The sticky TOC clipped under the sticky header on scroll.** `theme.mjs` now measures `.app-chrome`'s live height on load, resize, and font-load, and writes it to `--article-header-h`; `gm-prose.css`'s TOC `top`/`max-height` consume that var instead of a hardcoded `24px`. Live-witnessed in both themes: no overlap post-deploy.

**Mermaid diagrams had no click-to-enlarge and could overflow their column.** Wired a click handler in `theme.mjs` that clones the rendered SVG into the design SDK's existing `Modal` primitive via `ds.applyDiff`, reusing shipped `.ds-modal` CSS instead of a bespoke lightbox. Added `.mermaid svg{max-width:100%;height:auto}` in `gm-prose.css`. Live-witnessed: a dispatched click opens the modal on the deployed page in dark theme.

**Body copy measure and code/chip pill contrast were fixed in the design SDK, republished, and consumed.** `anentrypoint-design` bumped 0.0.468 -> 0.0.471; `site/package.json` repinned and rebuilt so the deployed page picks up the narrower `--measure` (68ch) and the `--bg-3` pill background step.

## 2026-07-31 - gm-config is now pulled from, not pushed to

**`gm-config` could never function as a real default source.** A nightly cron regenerated its `prose/`, `gates/`, `residual/`, and `fsm/` content from rs-plugkit's compiled defaults. It overwrote any hand edit on the next run. Deleted `sync-from-plugkit.yml` and its script. `gm-config` is now edited directly and pushed straight to main.

**Reads never actually reached `gm-config` in the common case.** `prose.rs` and two functions in `fsm.rs` hardcoded the wrong tier's cache-directory path. This is the default, zero-configuration case: a project with no explicit `.gm/config.source.json`. It resolves `gm-config` through a different cache directory. The hardcoded path pointed at an empty one. Every project silently served the compiled fallback forever, even while `gm-config` fetched and cached correctly for its own `gm.config.json` values. Added a `cache_dir` field to `Resolution` so every consumer reads the directory the actually-winning tier used. Fixed `prose.rs::read_from_config_repo`, `fsm.rs::source_repo_graph_path`, and `fsm.rs::resolve_hook_path`.

**A new signal distinguishes "gm-config is down" from "nobody overrode this key."** Both used to look identical: the compiled default served either way. `Outcome::ConfigRepoUnreachable` now names the reachability failure specifically, emits its own watcher-log event, and surfaces as `config_repo_unreachable` on every `instruction`/`transition` response.

**Verified live.** Cleared the local cache, dispatched fresh, and confirmed the resulting checkout carried this session's own `gm-config` fix (a stale-key correction) with no manual step.

## 2026-07-30 - bert wasm trap fixed, FSM phase-stuck-at-PLAN root-caused, paper-page layout fixes, DECIDE prose additions

**A forced-SIMD128 bug in the bert embedding plugin caused hundreds of embed failures per session.** The plugin called `gemm::set_wasm_simd128(true)` unconditionally. The release build never sets the `+simd128` target feature. The call forced a SIMD codepath the compiled wasm module never had. Every candle matmul call trapped. Removed the forced call. Live recall dispatches now succeed with zero embed failures.

**The FSM phase could get stuck at `PLAN`, a state name the current graph does not have.** `default_graph()` built a correct `policy` value with `initial_phase: "SPECIFY"`. The returned `Graph` struct used `Policy::default()` instead. This silently discarded the override. A fresh-prompt reset then wrote the serde-default `initial_phase` of `"PLAN"` into `turn-state.json`. The next transition had no legal edge from that state. It denied forever. Pulled the upstream fix. A fresh-prompt reset now correctly lands on `SPECIFY`.

**The gm site's home page clamped its content to a 940px column.** Empty space filled both sides of the window. A local CSS rule (`landing-capped`) duplicated a mechanism the design SDK already provides through the `AppShell` `narrow` prop. Removed the local duplicate. The SDK's own width logic now applies with no override.

**The gm paper page had five separate layout defects.** A live browser witness against the deployed site found each one. `h2`/`h3` headings jumped from 44px to 18px with no step between. `hr` and `blockquote` elements had no styling at all — a raw browser default gray line and plain unstyled text. The sticky table-of-contents rail did not stick. A local `overflow-y: auto` override on `html`/`body` broke its scrolling containing block. Prose links rendered the same white as body text, distinguished only by an underline. Fixed all five in the design SDK and gm's own site theme. Also fixed the SDK's `SDK_CSS_URL`/`SDK_JS_URL` constants. They pointed at unpkg's `@latest` alias and could serve a stale cached version for 20+ minutes past a fresh publish. They now pin to the exact installed SDK version at build time.

**DECIDE's browser-witness discipline gained two standing rules.** A local rebuild is not the deployed origin. A project with a real deploy target now needs a witness against that live URL, not only a local static server serving the same files. Fresh/anonymous state is not the only reachable state. A returning user with persisted auth/storage state is now a standing adversarial corner case, not an optional extra.

**The shared `agentplug-runner` daemon gave no queue signal.** A project's dispatch could sit queued behind other unrelated projects with no signal at all. The daemon's worker pool only wrote a project's `.status.json` heartbeat once its turn started running. A project waiting in the shared cross-project queue looked identical to a dead watcher. Added `queue_position`/`queue_depth` fields, written for every waiting project before the worker-pool batch starts.

**A CI `paths-ignore` rule silently skipped rebuilds for compiled prose edits.** rs-plugkit's `build.yml`/`release.yml` ignore `**.md` changes to skip wasted CI on real documentation edits. But every file under `crates/plugkit-core/src/orchestrator/instructions/prose/` is `include_str!`'d into the compiled binary — real served behavior, not documentation. A prose-only edit there never triggered a rebuild. Added a negated re-include pattern. A prose edit under that directory always triggers a build now. Genuine docs (`README.md`, `CHANGELOG.md`, `docs/**`) still skip it.

## 2026-07-29 - 8-stage FSM, ASD-STE100 prose rule, gm-config submodule

**The FSM changed from a linear 6-stage chain to a non-linear 8-stage graph.** The old chain was PLAN->EXECUTE->EMIT->VERIFY->CONSOLIDATE->COMPLETE. The new graph is SPECIFY->PROVE->EMIT->STATE->CONC->SEC->RES->DECIDE->COMPLETE. Every later stage can feed back to SPECIFY, EMIT, STATE, or PROVE. The pipeline dropped old vendored graph overrides. AGENTS.md, README.md, and SKILL.md now state the new stage list.

**ASD-STE100 (Simplified Technical English) now governs gm prose.** The rule sets short sentences, the active voice, and an approved word list. It applies to AGENTS.md, README.md, CHANGELOG.md, and SKILL.md files. It also applies to docs under `.gm/` and `gm-plugkit/instructions/`. Full rule text is at `.gm/disciplines/ste100/policy.md`.

**`gm-config` is now a git submodule of this repo.** It holds the default remote-config content: prose, the FSM graph, gate hooks, and policy. This adds an implicit default tier. A fresh gm install syncs from `AnEntrypoint/gm-config` by default. A project can still point at its own repo via `.gm/instructions/source.json`.

**README.md gained a section on configuring gm from an external repo.** It documents the `fsm-vendor` verb. It documents the three-tier prose order. It documents the trust risk of a config repo that runs gate-hook code.

## 2026-07-28 - browser verb health verified fleet-wide, gmsniff stale-registry bug fixed, comment-policy sweep

**gmsniff was silently reading dead daemon state, hiding any project (like `freddie`) that only registered after the daemon migrated to agentplug.** `readDaemonRegistry()`/`readDaemonStatus()` across `registry.js`/`index.js`/`cli.js`/`server.js` all pointed at `~/.gm-tools/daemon-registry.txt`/`daemon-status.json`, frozen since the daemon moved its live state to `~/.agentplug/` -- gmsniff only stayed usable at all via a separate readdir-over-`C:/dev` fallback that happened to still find most projects. Added an `AGENTPLUG_DIR` constant (same env-override pattern as `GM_TOOLS_DIR`) and routed every daemon-registry/daemon-status read through it, leaving `plugkit.version`/`gm-plugkit.version` on `GM_TOOLS_DIR` since those genuinely still live there. Live-verified: `freddie` now correctly appears under `--watchers --all` as idle.

**A user report that "the browser verb isn't working" resolved to already-fixed history, not a live defect.** Live-dispatched a real browser verb (clean 780ms round trip), checked 24h of fleet dispatch history for error signatures (none), checked for Chrome process pileup (none -- normal multi-process counts), and confirmed the installed `agentplug-runner` binary postdates the three browser fixes (`2ed8065` headless-default, `e86b393` CDP-hang-reject, `d3cff3e` orphan-sweep-all-roots) that landed in the ~36 hours before this session, all CI-green. The reports were almost certainly describing those now-fixed bugs.

**Comment-policy sweep across every agentplug file touched by concurrent sessions since the last pass.** `browser.rs` (viewport-prefix, orphan-sweep, and sandbox-retry doc comments converted to self-explanatory function names), `registry.rs` (plugin-reload rationale converted to named booleans and a renamed reinstantiate helper), `daemon.rs` (sweep-timing comment removed, self-explanatory const), `cdp_eval.js` (CDP-hang-reject and viewport-timing comments removed, functions renamed to carry the intent).

## 2026-07-25 - browser session-launch TOCTOU race fixed, codeinsight wall-budget/daemon-concurrency fixes

**A live TOCTOU race in `agentplug-host`'s browser verb caused a real ~20-Chrome-on-`about:blank` incident, root-caused and fixed.** `browser.rs`'s `run()`/`session_new()`/`session_close()` checked `sessions_map` for a live session, released the lock, then (on a miss) called `launch_chrome` -- a real Chrome spawn plus CDP-ready wait -- with no lock held, re-locking only to insert afterward. Concurrent dispatches for the same `cwd`+`session_id` key each independently saw the miss and launched their own Chrome before any reached the insert step. Confirmed the triggering project (spoint) never writes `.gm/exec-spool/.session-current`, so every browser dispatch resolved to the identical empty `session_id`, colliding every concurrent call from its active EXECUTE loop onto one key. Fixed with a per-key `session_lifecycle_lock_for_key` held across each function's full critical section, plus a bounded-growth eviction pass on the daemon's existing idle-reap cadence. Live-reproduced in a standalone concurrency harness: the original logic produced 20 distinct launches under 20 concurrent same-key dispatches (matching the report exactly); the fixed logic collapsed the same load to exactly 1.

**A separate live "plugin gm not loaded" eviction storm, also root-caused and fixed.** rs-plugkit's codeinsight wall budget (45s) exceeded agentplug's wasmtime dispatch deadline (40s), so a slow embed call (witnessed at 104-151s for a single file) blew the deadline before the budget check could fire, evicting the shared `gm` plugin slot daemon-wide. Compounded by `side_plugin_concurrency` defaulting to 1 against 117 registered projects sharing one daemon, starving the `bert` embed pool. Fixed by lowering the wall budget to 30s and setting `side_plugin_concurrency:4` in the daemon config.

## 2026-07-24 - final maximum-effort system-wide verification sweep, gmsniff vocabulary sync, ccsniff discipline audit, comment-sweep completion

A comprehensive system-wide audit across gm, ccsniff, and gmsniff to verify every subsystem at 100% working order. The gm harness itself was the primary verification surface: a full PLAN->EXECUTE walk with 32 PRD rows covering every aspect of the system (browser headless defaults, auto-update mechanics, config vendoring completeness, threading guards, sniffer detector parity, documentation currency, code quality sweeps, and the PRD/mutables configurability implementation).

**ccsniff discipline audit confirmed all 5 detectors working correctly** against 114 real transcript files (~40K events): search-discipline precision fix (known-path exemption) verified working (18 exempted, 0 flagged for gm), git-discipline found 1 real submodule raw-git-push violation, glyph-discipline found 3 decorative arrow glyphs, verb-bypass and spool disciplines returned zero false positives. No bugs found in ccsniff code.

**gmsniff vocabulary sync found and fixed real drift**: added 2 missing deviation kinds (spool-poll from poll_detect.rs, prd-resolve-duplicate-witness from prd.rs), removed 8 dead deviation entries, added 3 missing verb aliases (ps1, wait, sleep), removed duplicate python entry, updated subsystem tags from 6 dead tags (rs_learn, plugkit_wrapper, acp-launcher, learning, rs_codeinsight, rs_search) to 4 live ones (plugkit, hook, bootstrap, memory), fixed server.js recall-event filtering to use `memory` subsystem tag instead of dead `rs_learn`. All changes committed and pushed to gmsniff main.

**Submodule pointer consistency confirmed** across all 7 submodules with zero drift. Cascade pipeline health verified: plugkitVersion 0.1.962 matches across all 4 version files. claim-audit verb functional (scanned 216 lines, 0 stale claims). health verb lifecycle_liveness field confirmed working (last_event_age_ms: 928ms, stale: false).

**Code quality sweeps**: no UTF-8 BOM in any tracked source file, no graphical symbols anywhere, check-prose-bundle.mjs comments converted to self-explanatory function names (validateBundleCompleteness, extractBrowserModePrefixesFromProse, crossReferenceExecJsOptsFields, etc.). Script verified working with identical output. AGENTS.md confirmed clean (215 lines, 0 forbidden patterns). All stale agent files confirmed absent.

## 2026-07-23 - no-comments policy closed the loop, full-repo comment sweep, threading/pool fairness fixes, mutables lifecycle made configurable

A "we keep seeing paragraph-long comments recur" report exposed a real policy gap: AGENTS.md and the served EXECUTE/VERIFY prose prohibited comments without positively directing the replacement (self-explanatory naming/restructuring) or requiring an encountered pre-existing comment be converted on sight. Fixed both AGENTS.md and rs-plugkit's execute.md/verify.md served prose, then executed the resulting cleanup across the whole tracked source tree: every comment in rs-plugkit's plugkit-core crate, gm-plugkit's and bin's JS installers, and agentplug's host+runner crates (1768 lines in agentplug alone) converted to self-explanatory naming, with every genuinely load-bearing rationale (live-witnessed bugs, non-obvious invariants) preserved to the recall store before deletion rather than lost.

**A real, sustained "plugin gm not loaded" root cause found and fixed.** `agentplug-host`'s shared-plugin dispatch functions reused a pool slot's wasmtime Store/Instance even after a dispatch error -- unsafe per wasmtime's own documented contract, since a Trap leaves Store state unspecified. A single Trap in a shared plugin (bert especially, default pool size 1) poisoned that slot for every subsequent caller until idle-eviction or a full daemon restart. Fixed by evicting the slot on any dispatch error, forcing the next dispatch to instantiate a fresh Store. A second, independent fairness bug found while verifying the fix: `SharedPluginPool::acquire()`'s fallback poll loop only ever retried slot 0 for its full 20s timeout, ignoring other slots even if they freed first -- fixed to re-sweep every slot per poll iteration.

**The mutables lifecycle -- what counts as resolved, whether witness evidence is required, how many times a concurrent-write CAS retries -- is now project-configurable**, extending the same `Policy` externalization pattern already used for PRD-closed-statuses and initial/terminal phase names. Four new fields (default/witness status labels, a witness-requirement toggle, CAS max-attempts) thread through the real enforcement code in `mutables.rs`/`prd.rs`, each defaulting to the prior hardcoded behavior byte-for-byte so an unconfigured project sees zero change.

**A stray node.exe terminal-window flash on Windows, root-caused and fixed.** Both installer bootstrap scripts resolved `npm`/`npx` to their Windows `.cmd` shim (neither binary ships an `.exe`) and spawned it with `shell:true` -- `windowsHide:true` on that spawn only hid the immediate shim window, not the further node.exe child process the shim itself launches internally. Fixed by resolving the real `npm-cli.js` entry point and spawning the current node/bun executable directly against it with no shell layer at all, falling back to the old shim path only if the entry point can't be located.

**`gm-plugkit`'s own SKILL.md auto-refresh silently excluded `gm` itself** -- a hardcoded map entry pointed at a path (`gm-plugkit/SKILL.md`) that never exists, pre-claiming the lookup key and skipping the correct generic skills-directory scan that would have found the real `skills/gm/SKILL.md`. The single most important skill was never auto-refreshed by its own auto-refresh mechanism. Fixed by removing the bad hardcoded entry.

**`ccsniff`'s `--search-discipline` detector had a real precision gap**: it flagged every `Grep`/`Glob` call inside a gm session identically, with no distinction from gm's own documented exemption for known-path retrieval (grepping `.gm/prd.yml` or a spool response file directly is not open-ended discovery). Fixed to exempt `.gm/`-state-file targets and single-file `Grep` lookups; live-verified against real session data, findings dropped from 121 to 11 with the remaining 11 confirmed as genuine open-ended source searches.

## 2026-07-22 - dead gm-log observability restored, ccsniff/gmsniff detector gaps closed

A multi-turn system-wide sweep triggered by "check every aspect, fix any issues that arise" against the whole fleet, not just gm itself -- most of the real findings were in the sibling `ccsniff`/`gmsniff` observability tools, not gm's own code, but every fix still closes back through gm's own submodule pointers where it touched agentplug/rs-plugkit.

**`~/.gm/gm-log` had been completely dead for two-plus weeks.** Root cause was two independent, compounding bugs: (1) `agentplug-host`'s `host_log` import only `eprintln!`'d every message (discarded on process exit, and the `[agentplug:...]` wrapper prefix broke the `evt: ` line-start format rs-plugkit's own `emit_event()` sends) instead of appending to the calling project's `.gm/exec-spool/.watcher.log`, the mechanism the retired JS wrapper used to implement; (2) rs-plugkit's `emit_event()` was called at 55+ diagnostic/error/embedding sites but never once for the actual lifecycle classes (`dispatch.end`, `phase.transitioned`, `instruction.served`, `prd.added`/`prd.resolved`, `mutable.added`/`mutable.resolved`) that `gmsniff --list-sessions`/`--tree`/`--efficiency` depend on. Fixed both halves; live-verified `gmsniff --watchers` now correctly shows every agentplug-driven project (gm, spoint, casey, rs-plugkit, and others) as ALIVE, where before only stale legacy JS-wrapper leftovers appeared at all.

**`gmsniff --watchers`/`--conformance` were blind to the entire current-generation fleet.** `readWatcherStatus()` gated aliveness display on a `version` field the current agentplug shared-daemon `.status.json` shape never writes (it carries `runtime`/`shared_process`/`pid`/`ts` instead, since the wasm guest it serves updates independently of the runner binary). Fixed to treat `runtime==="agentplug"` as a valid alive signal; caught and corrected an own mid-fix mistake (an unrelated pre-existing uncommitted local diff had crept into the first commit attempt) before it shipped.

**`ccsniff`'s `--verb-bypass-discipline` and `--spool-discipline` detectors, previously believed shipped, had never actually landed in source** (a stale memory record claiming validation in a since-unverifiable commit) -- reimplemented both fresh: the first live run against real session history caught 24 genuine violations (raw `playwright`/`chromium` Bash calls inside a gm-driven workflow session) on its very first sweep. A false positive surfaced on that same sweep (the detector flagging its own commit message for merely mentioning "playwright" in prose) and was fixed same-turn (git-led commands are now exempt from the browser-lib-mention check).

**A real CLI unknown-flag bug in `ccsniff`** let an unrecognized flag silently swallow the *next* argument as its own bogus value (e.g. eating `--since`'s actual value), producing an unbounded 614MB+ scan instead of the intended filtered query; `--json`/`--ndjson` output was also wrongly exempted from the default 500-event cap. Both fixed and verified live.

**`browser` sessions now persist a real live Chrome process across dispatches, not just the on-disk profile.** Previously every `browser` dispatch launched then killed its own Chrome child, so only `--user-data-dir` survived between calls; a process-wide `SESSIONS` registry in agentplug-host now keeps the child process + its CDP port alive between dispatches, and `session new`/`session list`/`session close`/`session reset` are now genuinely parsed and honored rather than no-ops. The persisted profile directory also moved to a session-scoped path, `.gm/browser-chrome-profile-<session_id>/` (previously a single shared `.gm/browser-profile/`), and the launched Chrome is headful by default rather than headless. README.md's `browser` tool bullet was updated to match.

**Daemon heartbeat/dispatch-fairness fix plus a new `side_plugin_concurrency` config knob.** A long-running worker could starve the daemon's own heartbeat write under the prior scheme; fixed in agentplug alongside a new `side_plugin_concurrency` field in `~/.agentplug/daemon-config.json` letting bert/treesitter/libsql's shared-pool size be raised independently of `gm_concurrency` for deployments seeing real cross-project embed/search contention. `.gm/daemon-config-reference.md` and AGENTS.md's runtime-config-surface index both name the new field.

## 2026-07-21 - exec_js/browser profiling parity, FSM COMPLETE-gate bypass fix, codeinsight prune/overview bugs

A second pass the same day as the FSM/runner-self-update work below, driven by adversarial re-verification of everything that landed then ("check every aspect, fixing any issues that arise") rather than new feature requests -- every item here is a real bug found by exercising the previous pass's own claims live, not a speculative addition.

**`exec_js`'s `opts.mem`/`opts.profile` were documented but silently no-ops.** Both were specified in this file's own served prose (`{result, mem, wall_ms}` / `{result, profile:{timeframe,culprits}, wall_vs_cpu}`) but `agentplug-host`'s `exec_js.rs` had zero code reading either field -- dropped somewhere during the port from the retired JS wrapper, whose real implementation (a genuine `inspector.Session` CPU profiler + `process.memoryUsage()` sampling, sentinel-tagged onto stdout) was recovered from git history and ported into Rust nearly verbatim. Live-witnessed: a `fib(28)` profiled run now correctly identifies `fib` as the dominant self-time culprit (81%); a 1M-element array fill under `opts.mem` now returns real `rss_mb`/`heapUsed_mb`/`heapUsed_delta_mb`.

**A real COMPLETE-gate bypass**, found by deliberately dispatching an illegal `transition {to:"COMPLETE"}` straight from EXECUTE (8 PRD rows still pending, no EMIT/VERIFY/CONSOLIDATE traversed) -- it succeeded, reaching genuine COMPLETE phase. Root cause: `transitions.rs`'s `gate_rejection`/`gate_residuals` used `graph.edge_between(from,to)?`, where a *missing* edge (this graph has no direct EXECUTE->COMPLETE edge) `?`-short-circuited to "no gates to check" instead of "no legal path exists" -- backwards. Fixed both call sites to treat a missing edge as the strictest possible denial. Side effect once fixed: the AGENTS.md-documented "`transition to=PLAN` always legal from EXECUTE" behavior turned out to have been relying on the *same* bug (the default graph had zero backward edges at all) -- added real gate-free EXECUTE/EMIT/VERIFY->PLAN edges so that documented behavior is genuine rather than an accident of the thing that just got fixed.

**`browser`'s `capture`/`profile`/`trace` mode prefixes**, documented in this file's own served prose since before the sole-loader consolidation, had never been ported to the native direct-CDP host (`agentplug-host/src/browser.rs` + `cdp_eval.js`) -- only the retired JS-over-playwright wrapper ever had them. Implemented for real: `capture` wires `Runtime.enable`+`Network.enable` with a new general CDP-notification dispatch hook (the original `cdpSession` helper only ever routed id-keyed command *responses*, never id-less *events* like `Runtime.consoleAPICalled`); `profile` wraps the eval in `Profiler.start`/`stop` and aggregates the returned V8 CPU profile (same algorithm as the `exec_js` port above); `trace` opens CDP Tracing and buckets collected events by wall-clock into gpu/viz/compositor categories. Profile/trace artifacts are also written under `.gm/browser-profiles/` (git+npm ignored) for later inspection. Live-witnessed all three: `capture` caught a real `console.log`; `profile` correctly attributed a `fib(26)` call; `trace` correctly bucketed V8 GC/compile time on a tight loop.

**`codesearch`'s `vector_hits` response field was always empty** despite `mode:"fusion"` reporting real fused hits and `vector_top10` having real entries -- the response was returning the *raw* `rssearch_vector_hits()` lookup (an always-empty namespace, per an existing code comment explaining code-chunk embeddings are never mirrored into it), while `vector_top10` had already been fixed to fall back through the namespace that actually holds them. `vector_hits` just never got the same fallback. Fixed both `codesearch` return sites to report the already-resolved `vector_top10` value.

**A concurrent-write torn-read bug in `instruction`'s own PRD count.** Live-reproduced (not merely inferred): firing 20 rapid `prd-add` writes plus 15 concurrent `instruction` polls at the daemon simultaneously used to be able to make a poll land mid-write, fail YAML parsing, and silently report `prd_pending_count:0` -- the read-failure code path converted *any* non-zero return code into an empty list, discarding the actual error. Fixed with a self-correcting one-shot retry (a torn read resolves itself within milliseconds once the writer finishes) plus a loud log line if a failure genuinely persists twice. Re-ran the identical 20-write/15-poll burst post-fix: all 15 polls tracked the growing count correctly, zero spurious drops to zero.

**Two compounding `codeinsight` bugs that let deleted-file symbols live forever.** Found via a new `codeinsight_overview.likely_orphaned` field (a cheap text-occurrence dead-code heuristic, ported from a `mcp-thorns` comparison whose original "same-query" premise turned out to be a category error -- `mcp-thorns` is a one-shot whole-repo summary tool with no query argument, not a search tool to compare query-for-query) -- its first real run surfaced 20 candidates, every one of them from `gm-plugkit`'s already-retired-and-git-rm'd JS wrapper, which should have been long gone from the index. Root cause, two bugs: (1) the index's deleted-file prune step was gated on a *fully-complete* indexing pass, unreachable in practice on a repo this size (1500+ files, wall-budget-limited to partial passes every call); fixed by pruning against a separately-enumerated, much-higher-capped full file list, decoupled from the smaller per-call indexing work budget (the first fix attempt used the same capped list for both, which would have caused false-positive pruning of live files simply past the cutoff -- caught before shipping). (2) `rebuild:true`'s own `clear_codeinsight_full()` never actually cleared the real `code_chunks` SQL table, only KV-store namespaces -- so even an explicit rebuild silently preserved every stale row indefinitely. Fixed both; re-ran the same rebuild and `likely_orphaned` correctly dropped to zero stale entries.

**Commit-overview for submodule paths.** The already-implemented per-file "last changed `<sha>`: `<subject>` (+N-M, K files)" overview (real `git log --shortstat`, exact format match to this file's own prior spec) silently returned nothing for any file inside one of gm's new git submodules, because a submodule's *parent* repo only tracks its commit-pointer gitlink, never per-file history inside it. Fixed to explicitly skip (honest no-overview) rather than silently look broken; gm's own tracked files are unaffected and verified working directly.

Every item above was found by re-exercising a specific claim from the same-day work below with a real dispatch, not by re-reading source and reasoning it should work -- the FSM bypass and the codeinsight prune bug in particular were each initially "fixed" once, re-tested, and found still broken (a false-positive fix risk and a second, deeper root cause respectively) before the real fix landed.

## 2026-07-21 - dynamic FSM framework, runner self-update, configurable instruction sources, sole-loader consolidation

A week of major architectural work across gm, rs-plugkit, and agentplug, landing several long-standing structural asks.

**agentplug-runner is now the sole spool loader.** The redundant JS wasm-host wrapper (`gm-plugkit/plugkit-wasm-wrapper.js` + `gm-plugkit/wrapper/`), its supervisor, and the separate native `gm-runner` binary are all retired -- `agentplug-runner` (a real wasmtime host sharing one bert/libsql/treesitter instance across every project) is the only loader `bin/install.js` installs and the only one `gm-plugkit/cli.js` delegates to. This closed the "two agentplug-runner processes running" symptom (root-caused to `main.rs`'s spool-command fallback never contesting the shared-daemon lock before falling back to a private watcher) and a `libsql "database is locked"` bug (`sqlite3_close` vs `sqlite3_close_v2` in `agentplug-libsql`'s `Drop`).

**The FSM is now data-driven end to end, not hardcoded.** `Phase` changed from a closed Rust enum to a newtype wrapping any string; a project's `.gm/instructions/fsm/graph.json` (written by a new `fsm-vendor` verb) can declare its own phases, edges, and gates, replacing the built-in PLAN/EXECUTE/EMIT/VERIFY/CONSOLIDATE/COMPLETE chain wholesale if desired. Gate DECISIONS (not just message text) are now graph-driven: `gates.rs`'s previously-hardcoded CONSOLIDATE/COMPLETE checks were found to be silently pre-empting the new graph system entirely (a real bug caught while live-witnessing this row -- the graph edit had zero effect until the hardcoded path was retired in favor of routing through the same predicate registry). Gates can also be backed by a **jit hook** -- a real `exec_js` script run automatically at gate evaluation, whose `return` value (explicit return required; the script is wrapped in a function body, a bare trailing expression is discarded) gates the transition, fail-closed on any non-`true`/throw/missing-file outcome. Project-level policy (top-level-doc allowlist, long-gap threshold, gate-repeat-escalate threshold, etc.) that used to be hardcoded Rust consts is now part of the vendored, per-project-editable graph too.

**The runner self-updates its own executable, not just the wasm it serves.** Previously only the wasm guest hot-reloaded on a new release; the native binary required a manual kill/rebuild/redeploy cycle. `agentplug-runner` now polls for its own newer release on the same cadence, stages a verified download to `<exe>.new`, spawns it as a `takeover` process that builds a real engine and pre-warms plugins *before* signaling ready, and only then does the old daemon voluntarily release ownership -- a real handoff, not a race against staleness. Live-witnessed a full cycle including a real ownership handoff and continued dispatch service with no gap.

**Instruction prose now resolves through three tiers.** A local `.gm/instructions/<key>.md` override still wins first; new is a configured source-repo tier (`.gm/instructions/source.json` naming a repo/branch/subpath) that the daemon keeps synced via git clone/fetch+reset into a per-project cache, letting an org share a prose set across projects without forking rs-plugkit; the compiled default remains the final fallback. All three tiers mix per-key.

**Two daemon-hardening fixes** found while live-witnessing the jit-hook mechanism: an unhandled panic inside a dispatch previously unwound silently, dropping an already-claimed request with no response ever written (now caught, converted to a real error); a second pid-liveness race (separate from an earlier fix to the CLI's `ensure_daemon_running` path) existed in `claim_ownership`'s own heartbeat-timestamp check, live-hit as a direct `daemon` boot losing its claim to a pid that had already died -- both paths now verify the heartbeat's pid is a real live process, not just a recent timestamp.

**Everything-is-fixable doctrine rewrite.** `blockedBy: external` mutable status can no longer be used as a resting state for a solvable problem -- SKILL.md and served prose rewritten so an apparent blocker is itself a PRD row (build past it), never a stop. Concretely proved out on the browser verb: the playwriter relay's Windows `UV_HANDLE_CLOSING` crash, previously the canonical "external" example, was fixed by driving Chrome's CDP endpoint directly instead, bypassing the crashing relay (with playwriter kept as fallback when no CDP port is live).

**gm now has a submodule structure**: `.gitmodules` wires all cascade sibling repos (rs-plugkit, rs-codeinsight, rs-search, agentplug, agentplug-bert, agentplug-libsql, agentplug-treesitter), so `git clone --recurse-submodules` on gm brings the whole code structure in one step, live-witnessed via a genuine fresh clone.

Every item above was live-witnessed this session (real dispatches, real process handoffs, real file syncs read back) rather than asserted from source alone; several (the gate-decision bypass, the two daemon-liveness races, the missing-`return` hook-doc bug) were only found *because* the witness step was insisted on past an initial "looks right" reading.

## 2026-07-17 - reconcile drifted skills/gm/SKILL.md against the actually-served ~/.claude/skills/gm copy

Fix a real "served prose vs published source" staleness this session's own SKILL.md doctrine already names as a deviation to resolve immediately: the repo's `skills/gm/SKILL.md` (the file `bin/install.js` installs FROM) and the live-served `~/.claude/skills/gm/SKILL.md` (what an actual session reads) had genuinely diverged in both directions -- the served copy had frontmatter, the `browser` verb body-shape spec, the "Debug capture, GL error tracking" paragraph, and the four-bugs "Historical note, resolved" section that the repo copy entirely lacked; the repo copy had the "verification-is-a-build-task" paragraph, "Single-writer discipline," the `.turn-summary.json`/`instruction`-response phase-disagreement note, the Reboot-loop-escape section, and the "This file is not gm's whole procedure" closer that the served copy lacked. Neither file was a strict subset of the other -- each had unique content the other was missing, meaning at least one side was hand-patched directly at some point and never merged back to source (the same drift class `bin/install.js`'s own guard, landed 2026-07-16, exists to prevent for the WRAPPER file, but nothing equivalent existed for SKILL.md). Reconciled into one superset landed in the repo (source of truth) and mirrored to the live `~/.claude/skills/gm/SKILL.md` install path in the same pass so the authoring session itself benefits immediately. Verified ASCII-clean (0 non-ASCII chars), no BOM, valid frontmatter (`node` string-check, matching the discipline already established in the 2026-06-25 "sharpen skill dispatch-batching rule" entry above).

## 2026-07-17 - GL error capture: fix the fill-once-then-frozen cap + add stack traces + a GPU-tracing proactive hint

Fix a real debugging-productivity gap hit live while root-causing a game-engine FPS regression: the `browser` verb's always-on `window.__gmGlErrors` capture (`plugkit-wasm-wrapper.js`'s `addInitScript`-installed `drawArrays`/`drawElements`/`-Instanced` wrapper) recorded the first 40 raw OCCURRENCES of any GL error, ever, per page load, then silently stopped -- on a bug that fires every single frame, the array filled inside the first second and every subsequent `browser` dispatch for the rest of a long debugging session (dozens of dispatches spanning many minutes) read back the exact same frozen 9-ish entries, making a still-firing-every-frame error look capped, stale, or already-resolved. Fixed to a per-signature (draw-fn + error code + mode + count + instanceCount) DEDUP table: a recurring error updates its own entry's `occurrenceCount`/`lastDrawCallIndex` instead of being dropped once 40 raw occurrences have ever logged, so growth is now bounded by DISTINCT error shapes (realistically a handful) rather than raw volume, and a caller can tell "still happening, N times, most recently at draw #X" instead of a dead list. Also added: `window.__gmGlErrorTotalCount` (a true cumulative counter, exposed in every response's `debug.gl.errorTotalCount`, independent of the dedup table's 40-signature cap); a real captured JS stack trace (`stack`, last 8 frames) on each distinct error's first occurrence -- previously finding the triggering call site required hand-rolling the exact same `gl.drawX=function(){...new Error().stack...}` monkeypatch from scratch every session, and since this wrapper's own `getError()` drain runs before any user script can react, a user-installed monkeypatch of the same shape always read back `NO_ERROR` (the single-slot GL error queue was already drained) -- `window.__gmGlLastDrainedError` now exposes the wrapper's own last-drained `{fn,error,errorName,drawCallIndex}` so a user script can read the ALREADY-drained result directly instead of racing a second `gl.getError()` call that can only ever see zero.

Separately, `browser`'s `profile\n<script>` / `exec_js`'s `opts.profile:true` CPU-sampling path (`aggregateCpuProfile`, shared between both surfaces) now returns a `gpu_hint` field alongside `culprits` when the top culprit is the unattributed `(program)`/`(native)` bucket at >=40% self-time -- on the browser surface this proactively names the `trace\n<script>` CDP-tracing follow-up (real `gpu_us`/`viz_us`/`cc_us` wall-clock GPU-process activity, which the CPU sampler cannot see), saving a full extra dispatch+re-read round trip every time a caller has to independently rediscover "the CPU sampler is blind here, GPU tracing is the next step" -- exactly the detour taken mid-session before this fix existed. On the `exec_js` node surface the hint is worded correctly for that context instead (no GPU-tracing follow-up applies to a pure Node script; points at `opts.mem:true` / narrowing the profiled span instead).

Live-witnessed via a standalone Node re-implementation of the shipped dedup/counter logic (500 identical recurring errors -> 1 distinct entry with `occurrenceCount:500`, `totalErrorCount:500`; a second distinct signature immediately gets its own entry rather than being starved by the cap; 45 further distinct signatures correctly cap at 40 total entries) and the shipped `aggregateCpuProfile` template extracted and executed directly against synthetic CDP profile data (dominant-`(program)` case correctly returns the browser-worded `trace`-pointing hint when `isBrowserCtx=true` and the node-worded no-GPU-follow-up hint when `false`; a non-dominant case correctly returns `gpu_hint: null`). `node -c` confirmed the modified `plugkit-wasm-wrapper.js` parses cleanly.

Also fixed a real contradiction in `skills/gm/SKILL.md`: line 67 banned "poll with `sleep && ls`" unconditionally, while the very next lines (69/71) describe legitimate scenarios requiring exactly that kind of wait on a genuine `busy_until` window -- a session hitting a long `browser`/`exec_js` dispatch had no sanctioned wait primitive and had to reinvent one under the ban's shadow. Added an explicit carve-out distinguishing a blind unconditional sleep (still banned) from a bounded wait on an already-advertised `busy_until` window (licensed), naming the concrete shape for both a background-monitor-capable host (a condition loop over the OUT file's existence handed to that primitive) and a host without one (a single bounded sleep no longer than the declared remaining window, never chained/repeated).

## 2026-07-16 - guard shared ~/.gm-tools wrapper install from gm-repo-local overwrites

A direct `node gm-plugkit/cli.js` invocation against the `C:/dev/gm` source checkout (bypassing `bun x gm-plugkit@latest`, which fetches an isolated npm copy) copied the repo's local dev-tree wrapper over the single machine-wide shared `~/.gm-tools/plugkit-wasm-wrapper.js` install every project's watcher depends on, cascading a `wrapper-sha-drift` kill+respawn across 10+ unrelated project watchers within seconds. `bootstrap.js`'s `ensureWrapperFresh()` now detects a gm-source-repo invocation (`package.json` name `gm-skill`) and refuses the shared-install overwrite unless `GM_PLUGKIT_ALLOW_DEV_WRAPPER_OVERWRITE=1` is explicitly set. The normal `bun x gm-plugkit@latest` user path was already unaffected (confirmed live: `wrapper_refreshed:false` from inside the repo).

## 2026-07-16 - stop gm-plugkit CLI spool command from blocking on network round-trips during watcher reboot

`gm-plugkit/cli.js` now spawns the daemon immediately whenever a local wasm+wrapper already exist, deferring the remote-freshness check (`ensureReady()`'s `probeSelfStaleness`+`resolveLatestRemoteVersion`+possible re-download) to a detached background process instead of awaiting it before every daemon spawn. This applied unconditionally even on the version-drift-reboot and wrapper-drift-reboot paths, which is what held up the calling CLI/terminal. Live-witnessed: cold-boot-no-prior-status path 454ms (was multi-second-to-30s); version-drift-forced taskkill-then-respawn path 1393ms including the taskkill (was 5.5s+ minimum before reaching `startSpoolDaemon`).

## 2026-07-12 - fix exec_js default path: top-level `return` was a SyntaxError; wrap in async IIFE + capture result

Fix the `host_exec_js` default (non-mem, non-profile) path in `gm-plugkit/plugkit-wasm-wrapper.js` so `exec_js {code:"return 2+2;"}` runs. Root cause (live-witnessed): the default path did `spawnSync(process.execPath, ['-e', code])`, injecting the user's code verbatim into `node/bun -e` where a top-level `return` -- the documented gm exec_js contract ("return the result") -- is a SyntaxError (`Return statements are only valid inside functions`), so every `return`-bearing dispatch failed with exit 1 (or, on an older stale watcher, a spurious SIGTERM read as `timed_out`). The `mem`/`profile`/`browser` paths already wrapped user code in an async IIFE and captured the value; the default path was the sole outlier that did not. Fix: wrap the default-path code in `(async () => { try { const __r = await (async () => {<code>})(); console.log('__GM_RESULT__'+JSON.stringify(__r)); } catch (__e) { console.error(__e.stack); process.exitCode=1; } })()` -- so top-level `return` and top-level `await` both work, the returned value is surfaced as a `result` field via a `__GM_RESULT__` sentinel (stripped from the caller's stdout, matching the browser verb's mechanism), and a throw still lands its stack on stderr with exit 1 (documented error channel preserved). Two hardenings rode along, shared across all three branches: (1) `timed_out` now requires `signal==='SIGTERM' && duration >= 90% of timeoutMs`, so a spurious sub-deadline kill (the old SIGTERM-at-28ms) is no longer mislabeled a timeout; (2) `spawnSync`'s `result.error` (spawn race / ETIMEDOUT / EBADF / ENOENT) is surfaced as a `spawn_error` field so a `status:-1` is diagnosable instead of a silent empty failure. Witnessed live through the webix `.gm/exec-spool`: `return 2+2` -> `{ok:true,exit_code:0,timed_out:false,result:4}`; `console.log("side output"); return {sum:42}` -> `{ok:true,stdout:"side output",result:{sum:42}}`; `throw new Error("kaboom")` -> `{ok:false,exit_code:1,stderr:"Error: kaboom...",timed_out:false}`; top-level `await` -> `result:42`. Also verified standalone on both node v24.15.0 and bun 1.3.8 across return/log+return/await/throw/undefined/bignum. Source is what ships; the installed copy at `~/.gm-tools/plugkit-wasm-wrapper.js` was resynced and the watcher rebooted onto it (sha-confirmed) for the live witness.

Diagnosing the above surfaced a second, separate defect that made the spool nearly unusable on a large repo: an unbounded watcher restart loop. Root cause -- the boot warmup at watcher start runs a synchronous `codesearch` dispatch that, on an absent/stale codeinsight digest, triggers a full in-wasm embed of the whole project. That embed is a blocking wasm call, so the 5s heartbeat `setInterval` cannot fire while it runs and the heartbeat freezes. The warmup only reserved a 360s `busy_until` window; on a large repo the cold embed exceeds it, so `busy_until` expires while the loop is still blocked, the supervisor's 30s stale-heartbeat check kills the watcher mid-embed, the new digest never persists, and every respawn re-embeds from scratch -- spawning duplicate watchers that contend on the shared libsql index and never converge. Fix: raise the boot-warmup `busy_until` window from 360000 to 1200000 ms so it comfortably exceeds a worst-case cold embed. The supervisor already returns early from its health check while `busy_until > now`, so the longer window is sufficient to let one embed complete; it is a one-time cost because the digest persists after a single completion (subsequent boots skip the reindex, so their warmup is near-instant). Both fixes ship in the same `plugkit-wasm-wrapper.js` change.

## 2026-06-25 - belt-and-suspenders canvas clamp in theme.mjs render-style; drain client-debug AGENTS entry to recall

Add a `#ds-article-host canvas { height:300px !important; max-height:300px }` clamp to the `site/theme.mjs` render-mode shipping `<style>` and drain the `Client-side debugging exposes globals` AGENTS.md entry to a one-line rs-learn pointer. Why: the SDK `.ds-prose canvas` clamp is the primary fix, but the render-mode `<style>` is the only same-repo surface that survives `extractArticle` (which strips head `<style>`, `<header>`, `<footer>`), so an additive host-scoped clamp guarantees the bound even if a future article mounts outside `.ds-prose`. Live-witnessed on deployed `/gm/stats/` via the `browser` verb page.evaluate: all 3 canvases stable at `rectH=300` / `styleH=300px` across two reads 4s apart (no growth), `drawn=3`, all 5 `/gm/docs/api` endpoints 200. The AGENTS drain is the standing memorize back-pressure -- the substance fired to the default namespace, the paragraph compressed to a `recall:` pointer in the same commit.

## 2026-06-25 - clamp stats chart canvases via the SDK (stop Chart.js infinite-grow)

Add a `.ds-prose canvas` height clamp (300px) to the `anentrypoint-design` SDK `gm-prose.css` so the deployed stats dashboard charts stop growing unbounded. Why: residual triage surfaced an uncommitted local fix in `docs/stats.html`'s `<style>`, but the site build strips `<style>` from the page source (deployed `articleHtml` is only `<main>`+`<script>`; `dist/stats/index.html` carried zero `height:300px`), so the rule was inert -- the deployed `starsChart` canvas measured 3215px (browser-258). Per the SDK pro-rata rule, a chart-canvas sizing rule is a visual-component style that belongs in the SDK, not the site; the inert `stats.html` edit was reverted and the rule added to `gm-prose.css` (literal-free), published so `@latest` serves it.

## 2026-06-25 - fix stats dashboard API base under the GitHub Pages project subpath

Derive the stats dashboard `API_BASE` from `location.href` instead of the root-absolute `/docs/api`, and copy the data JSON into the deployed subpath in `gh-pages.yml`. Why: the gm site is served under the `/gm/` Pages project subpath, so a root-absolute fetch base ignored the prefix and every endpoint 404'd, leaving all metrics at `--`. The location-derived base resolves to `/gm/docs/api/` on Pages and `/docs/api/` at root. Live-witnessed on the deployed `/gm/stats/` via the `browser` verb page.evaluate (cache-busted navigation): `API_BASE=https://anentrypoint.github.io/gm/docs/api`, all 5 endpoints fetch 200, `fetchError` null, real numbers (npm 26,617 / open-issues 1 / commits-per-week 1011 / contributors 7). A stale browser-cache hit had masked the live deploy; a cache-busting `?cb=` re-navigation separated browser-cache from deploy-lag.

## 2026-06-25 - consume design SDK pro-rata; remove all local CSS overrides from site theme.mjs

Migrate every themed visual component out of the `site/theme.mjs` `<style>` override and into the `anentrypoint-design` SDK, so the site uses the SDK as-is and graphic-design changes centralize upstream. Why: the directive is to use the SDK pro-rata and never override it -- any design change goes into the SDK itself. The TOC sidebar grid (`#ds-article-host.has-toc`), `.cli` block, `.panel .row`, `.ds-prose` cards/tags/`.gm-callout`, and ds-prose typography were duplicated as a ~7KB local override (the prior session had judged them "load-bearing and kept" -- now reversed); they are added to the SDK as a token-only `gm-prose.css` (registered in the SDK build + lint-tokens, literal-free), the `--panel-hover` token added, and the SDK published as `anentrypoint-design@0.0.224`. The local `theme.mjs` `<style>` is reduced to 1318 chars of render-mode plumbing (flatspace `article-flow`/`landing-cap` html-class toggles, crumb media query) plus site article-layout rhythm that is not an SDK component; 6 stale comment blocks and hardcoded hex fallbacks stripped. Witnessed across all 7 routes (home/paper/crates/skills/distribution/made-with/stats) via the `browser` verb page.evaluate after override removal: TOC grid `256px 656.8px` sticky, `.cli` JetBrains Mono with accent-green prompt, callout border-left, `--panel-hover` token all still render -- from the SDK, with clean console and only unpkg `anentrypoint-design` css/js in network.

## 2026-06-25 - sharpen skill dispatch-batching rule with this-run's missed-bundling patterns

Rewrite the SKILL.md line-59 "Batch writes and reads together" rule into a default-batching imperative grounded in concrete misses audited from this session's own tool-call history. Why: the prior rule was correct but under-specified, so independent dispatches still got issued serially and same-file multi-edits still raced. The sharpened clause frames one block as the default and the serial single dispatch as the drift, enumerates the independent-dispatch classes that batch (N prd-add/prd-resolve/mutable-add, the orient recall+codesearch, inspection Read/codesearch), and bounds the rule with the two edges this run exposed: same-file multi-Edit in one block is not fan-out (the first Edit invalidates the read-state, the rest fail "File has been modified since read" -- collapse to one Edit/replace_all/Write, batch only across DIFFERENT files), and a long verb whose response is not ready gets the wait probe AND the re-Read in one block (the `until [ -f out/<verb>-<N>.json ]` loop plus the Read together, or an advertised `busy_until`), never a bare wait turn then a separate Read turn; homogeneous fan-out responses are read all-at-once or spot-checked first+last. Kept implicit/elicitation per the skill-prose discipline (non-derivable ABI -- the failure string, the until-loop path -- stays explicit; derivable procedure stated as principle), ASCII-clean, no BOM, witnessed by exec_js (nonAsciiCount 0, bom false, 88 lines / 20676 bytes). Skill-only, no served-prose edit: batching is dispatch-transport mechanics whose home is the SKILL.md dispatch cluster, and the user asked for it in the always-visible skill file. The installed copy mirrors via the bootstrap sha256-rewrite.

## 2026-06-25 - paper gen-3 currency pass + site-render triage

Update `docs/paper.html` ("The Complete Discipline") to match the current gen-3 codebase after a long stretch without a content refresh. s5 cross-session memory rewritten from the stale file-based MEMORY.md model to the file-vec rs-learn store (memorize-fire embeds via in-wasm bge-small, writes `.gm/disciplines/<ns>/<key>.json` + `-vec`, recall/auto_recall score cosine x recency), keeping the user/feedback/project/reference taxonomy. s4.2/s10 parallelism corrected from "up to 3 concurrent subagents" to N parallel spool dispatches bounded by the spool concurrency cap (platform subagents forbidden). Normalized every narrative `exec:<verb>` to the bare verb (codesearch/recall/browser/exec_js/pause) matching s30 and the served prose. s6.3 output shape fixed to the single `out/<verb>-<N>.json` (nested) / `out/<N>.json` (root) envelope. Stale `gm-starter/` paths corrected to `lib/` + root `gm.json`; s33 title and TOC renamed Daemonize-by-Default -> Synchronous-Under-Wasm; s4.3/s30 "Stop-Hook" vocabulary -> "Stop-Checkpoint" reconciled with s39; s41 wasm size `~149 MB` -> `~143 MB` (bge-small "twelve hidden layers" preserved); VERIFY exit "CI green" reconciled to clean+pushed with the cascade build running async post-session; header/paper.yaml date May -> June 2026. Witnessed by a flatspace rebuild and a `browser` verb page.evaluate on the built `/paper/`: all corrected strings present, all stale strings absent, article renders, 48 sections, zero console/page errors, SDK resolving anentrypoint-design@0.0.222.

Resolved the inherited site-render family: the gh-pages-deployed SDK is runtime-loaded from unpkg `@latest` (now 0.0.222), so the build-time `^0.0.221` pin in `site/package.json` only feeds flatspace aggregation and cannot pin the shipped SDK; theme.mjs overrides confirmed load-bearing and kept; degenerate routes (`/`, `/made-with/`, `/skills/`) render with empty console/pageErrors.

## 2026-06-25 - browser verb surfaces about:blank instead of silently evaluating it

Make the browser verb non-surprising when a dispatch never navigates. `plugkit-wasm-wrapper.js host_browser_exec` now injects a `blankProbe` into the profile/capture/bare eval paths when no `url=`/bare-URL navigation was requested: if `page.url()` is `about:blank` (or empty) it emits a `__GM_BLANK__` marker, which the wrapper reads off the runner stderr to set `navigation_requested:false`, `landed_on_blank:true`, and a `hint` telling the caller to prefix `url=<target>`; the marker is scrubbed from the returned stderr. Why: navigation was opt-in, so a bare expression on a never-navigated session evaluated against an empty page and looked like the tool "only opens about:blank" -- the envelope now says so out loud instead of returning silent empties. The default navigating path is byte-unchanged. rs-exec/rs-plugkit Rust carry no browser-navigation logic (witnessed: zero grep hits in rs-exec/src, only served prose in rs-plugkit); the fix is purely wrapper JS. Also removed the false `session=<id>\n<expr>` body shape from the served `browser.md` (it was never parsed by the wrapper and ReferenceErrored at runtime) and documented the new envelope fields; synced to `rs-plugkit/src/orchestrator/instructions/browser.rs`.

## 2026-06-25 - rename skill dir to gm; lib-free installer; terse paper

Rename the skill directory `skills/gm-skill` to `skills/gm` so the slash command is `/gm`; the npm package id stays `gm-skill` (permanent) and the `AnEntrypoint/gm-skill` mirror keeps receiving `skills/gm/SKILL.md`. Why: the directory name is the command, and `/gm` is the surface users invoke.

Add `bin/install.js`, a cross-platform (mac/linux/windows) installer that drops the skill directory into `~/.claude/skills/gm/` (or `.claude/skills/gm/` with `--project`) with no `npx skills` library and no marketplace. It has interactive and non-interactive (`-y`/`--yes` or non-TTY) variants; non-interactive SETS four Claude Code settings (`autoCompactEnabled:true`, `autoCompactWindow:380000` -- an absolute token count = 38% of 1M, not a percentage -- `effortLevel:"low"`, `alwaysThinkingEnabled:false`) and explains how to revert, while interactive OFFERS them. Why: the settings tune the harness for execution-as-reasoning -- the LLM still thinks, it tests its thoughts in code rather than in long thinking blocks. `test.js checkRenameAndInstaller()` guards the rename + installer end-to-end against an isolated temp HOME.

Terse-rewrite `docs/paper.html` to drastically improve brevity while preserving every technical claim, code identifier, reference superscript, figure, and the signal-to-noise framing. Pepper `gm-plugkit/instructions/*.md` with implicit reasoning-in-code framing (synced to the rs-plugkit orchestrator consts, which push to main to trigger the cascade). Drain two single-platform mechanism paragraphs (wrapper-ESM, atomic-guard) from AGENTS.md to rs-learn pointers per the bidirectional-migration back-pressure.

A targeted dna-sweep (scoped to rs-search + rs-exec, with an ALREADY_SHIPPED constant added so prior fixes classify out) surfaced a cluster the earlier whole-repo sweep had buried: gm shipped a dead-and-fake alternate WASM host. Deleted `lib/wasm-host.js` (host imports returned constants regardless of input, `hostExecJs` `eval()`-ed strings read from WASM memory, the `node-fetch` import was absent from dependencies, `readString` truncated at a hardcoded 1024 bytes, and the `host` import namespace meant it could never drive the real wasm) and `bin/plugkit.js` (which routed every verb to that stub; it was not in `package.json` `bin` and no hook referenced it). Also deleted the dead legacy browser modules `lang/browser.js` (no `exec.match`, targeted the stub), `lib/browser.js`, and `lib/browser-spool-handler.js`. The sanctioned WASM host is `gm-plugkit/plugkit-wasm-wrapper.js` via `bun x gm-plugkit spool`; browser routes through the wasm `browser` verb.

Hardened the survivors: the `spool-dispatch.js` mutables gate now parses `status:` via `yamlStatusValues` like the stop gate (one definition of unresolved); `test.js` `checkNoComments` flags leading block comments too, template-literal-aware so CSS-in-template stays exempt; `skill-bootstrap.js` resolves the watcher runtime via `process.execPath` for both spawn paths (no hardcoded `bun`) and caps `httpGet` redirects at 5; `bin/bootstrap.js` `acquireLock` sleeps via `Atomics.wait` instead of spawning a node subprocess. rs-search: README rewritten to describe the live unweighted `rrf_merge_n` fusion honestly (the weighted/normalized variant stays unwired pending a search-quality eval) and the context-symbol regex `SKIP` set expanded so `return`/`await`/`match`/etc. are not captured as enclosing symbols. rs-exec: dropped the unused `anyhow` dependency. rs-codeinsight: removed a redundant second `config::load_config` read. Seven host-ABI / search-quality-eval / multi-host-boot / timing-contract findings are recorded in recall for a supervised session.

## 2026-06-21 - dna-sweep workflow + 12-principle improvement fixes across gm/rs-*

Add `.claude/workflows/dna-sweep.js`: a reusable fan-out that sweeps gm and the rs-* siblings through the 12-principle synthesized-engineering-DNA lens plus the context-engineering research (chatgpt-memory, context-rot, Recursive Language Models), find -> adversarially-verify -> classify(implement-now | defer-invasive) over per-repo agents. Parameterized (`args.repos`, `args.focus`) so the sweep is not re-authored each run; the known invasive-infra clusters are encoded as a constant the classifier auto-defers. One run surfaced 101 candidates -> 16 verified -> 14 implement-now, all shipped.

gm fixes: `spool.js` validateVerb/validateLang throw on unknown input instead of coercing to status/nodejs; `spool-dispatch.js` session-scopes the instruction-seen check, gates PRD/mutable state via a line-aware `status:` parser (fail-closed, ignores comments and description text), and drops the "paper section N" citations from gate-denial reasons; `skill-bootstrap.js` never verifies a newer download against the manifest's older-version hash and times out the ps/powershell pid probes (3s); `ssh.js` marks timed-out output as partial; `browser-spool-handler.js` error names the real failure (watcher liveness) not a defunct rs-exec:5000; `gm-validate.js` serves the repo's own `docs/` fixture instead of a hardcoded author path; dead `renderPlatformSkill` removed; code comments stripped from `bin/`, the wasm wrapper, and `site/theme.mjs`, with a `test.js` checkNoComments guard added. rs-codeinsight: `project.rs` reads package.json top-level fields via a string-aware depth-tracking parser (no first-substring corruption) and drops the unused `walkdir` dep. rs-plugkit: `code_index.rs` write_chunk and memorize_at_finalize use bound parameters instead of `sql_quote` string interpolation. Two findings (recall.rs BGE-prefix rerank mismatch, supervisor single-instance proceed-on-failure) are tracked as blocked PRD rows for a supervised session.

## 2026-05-21 - browser launches headed by default

`gm-starter/gm-plugkit/plugkit-wasm-wrapper.js::startManagedBrowser` drops the hardcoded `--headless` arg from the runner invocation. The launched browser is visible by default; `GM_BROWSER_HEADLESS=1` opts into headless. Same patch lands in `~/.claude/gm-tools/plugkit-wasm-wrapper.js` for the deployed copy. The user can always see the window the agent spawned.

## 2026-05-21 - observability + supervisor + browser-name scrub

gmsniff and ccsniff CLI tools (`bin/gmsniff.js`, `bin/ccsniff.js`, mirrored under `gm-starter/bin/`) implement the observability surface AGENTS.md documented but that did not ship. gmsniff flags: `--embed-failures`, `--recall-misses`, `--recall-scores`, `--classifier-rejects`, `--memory-leverage`, `--recall-modes`, `--table-drops`, `--discipline-sigil-ignored`, `--since <Ns|Nm|Nh|Nd>`. ccsniff flags: `--git-discipline`, `--learning-xref`. Both pipe-friendly, no external deps, tolerate missing logs. `gm-starter/bin/plugkit-supervisor.js` separates supervision from the wasm watcher: monitors `.status.json` mtime, SIGTERMs the child on stale heartbeat with `.shutdown-reason.json` writeback, exponential backoff respawn (2s->30s cap), single-instance guard via `.supervisor.pid`. `gm-starter/lib/skill-bootstrap.js::spawnPlugkitWatcher` routes through the supervisor; `ensureNextStepWiring` seeds `.gm/next-step.md` plus `CLAUDE.md`/`AGENTS.md` `@-include` lines on every bootstrap so the file the harness expects always exists. `gm-starter/gm-plugkit/plugkit-wasm-wrapper.js`: auto-recall TTL cache (30s) and skip-fallback-on-primary-hit remove doubled BertModel.forward on turn entry; 10s slow-backoff disables the path for 5 min after a slow firing. Browser tooling renames remove playwright/puppeteer/playwriter from agent-facing surfaces: `findPlaywriter`->`findBrowserRunner`, `runPlaywriter`->`runBrowserRunner`, `PLAYWRITER_BROWSER_{PATH,KEY}`->`GM_BROWSER_RUNNER_{PATH,KEY}` with back-compat reads, scrub regex broadened to all three names. `watcher.silent-abort` now writes `.silent-abort-forensics.json` snapshotting `prior_verb_active`, `prior_status`, OS-level `prior_pid_alive` probe so gmsniff can correlate external kills.

## 2026-05-17 - WASM-only conversion: native plugkit retired, single wasm32-wasip1 artifact

All 5 Rust crates (rs-plugkit, rs-exec, rs-search, rs-codeinsight, rs-learn) converted to WASM-only cdylib. Native binaries no longer built or published. `plugkit.exe` removed; `plugkit.wasm` (~150KB) replaces it loaded by `plugkit-wasm-wrapper.js` running under node or bun. JS host implements 15 host functions: host_fs_* (read/write/exists/mkdir/readdir/stat/remove), host_kv_* (get/set/delete/list), host_fetch, host_exec_js, host_vec_search, host_vec_embed, host_env_get, host_log, host_now_ms (host_browser_* removed in a followup change). Orchestrator FSM (PLAN->EXECUTE->EMIT->VERIFY->COMPLETE) reachable from wasm `dispatch_verb`; pkfs abstraction routes std::fs through host_fs_* under wasm32. Cascade pipeline: any rs-* repo push -> rs-plugkit release.yml builds wasm -> publishes to plugkit-bin Releases + npm `@anentrypoint/plugkit-wasm` -> `gm.json::plugkitVersion` + sha256 manifest auto-bumped. 6-target test matrix in rs-learn collapsed to single wasm32-wasip1 build. 5 npm platform packages (`@rs-codeinsight/*`) replaced by single `@anentrypoint/rs-codeinsight-wasm`.

## 2026-05-17 - orchestrator-in-plugkit migration; gm-skill becomes canonical harness

gm state machine (PLAN->EXECUTE->EMIT->VERIFY->COMPLETE) migrated into rs-plugkit at `rs-plugkit/src/orchestrator/{mod,state,transitions,mutables,memorize}.rs`. plugkit exposes four new verbs: `transition`, `mutable-resolve`, `memorize-fire`, `phase-status`. All harnesses delegate phase/mutables/memorize logic to plugkit rather than reimplementing it skill-side. Skills collapse to 1-line spool-dispatch stubs that call the corresponding plugkit verb. gm-starter/skills/gm-skill/SKILL.md becomes the canonical universal harness body; the 10 platform-specific skills (gm-cc, gm-gc, gm-oc, gm-codex, gm-kilo, gm-copilot-cli, gm-vscode, gm-cursor, gm-zed, gm-jetbrains) become thin re-export wrappers with zero divergent orchestration prose. Tool surface across all skills reduced to `Skill, Read, Write` -- Write permitted only for spool dispatch. Code execution, git, browser, recall, memorize, codesearch all route through `.gm/exec-spool/` and are serviced by plugkit verbs.

## 2026-05-17 - consolidation: skill cache via TemplateBuilder inheritance

Eliminated 8x skill file duplication across bundled platforms (cc, oc, kilo, codex, copilot-cli, vscode, cursor, zed) via TemplateBuilder inheritance chain. TemplateBuilder.loadSharedSkills(sourceDir) reads 4 core skills (gm, gm-execute, gm-emit, gm-complete) once, caches in Map. TemplateBuilder.selectBundledSkills(platformName) identifies bundled platforms; AutoGenerator loads shared skills before platform loop and passes cache to each bundled adapter. Platform constructors accept optional `skillsCache` parameter; loadSkillsFromSource() uses cache when available, falls back to disk read for external-skill platforms (gc, qwen, hermes, thebird, antigravity, windsurf, jetbrains). TemplateBuilder.mergeSkillMetadata(skillContent, platformName) prepared for future per-platform overrides without re-duplication. Verified: all 15 platforms build, all 4 core skills remain byte-identical across 8 bundled platforms post-consolidation. Benefit: single parse + reuse vs 8 independent read operations. Extensibility: metadata merge path available for platform-specific tweaks.

## 2026-05-14 - gm-skill: daemon-bootstrap.js for standalone daemon orchestration

gm-skill now provides gm-skill/lib/daemon-bootstrap.js, a standalone daemon orchestration module with zero plugkit dependency. Exports five functions: `checkState(daemonName)` detects running daemons via platform-specific mechanism (tasklist on Windows, pgrep on Unix); `spawn(daemonName, cmd)` spawns detached daemons via bun; `waitForReady(daemonName, host, port, timeoutMs)` polls socket reachability with configurable timeout; `getSocket(daemonName)` retrieves socket address from status file; `shutdown(daemonName)` kills daemon process tree. All functions emit JSONL events to ~/.claude/gm-log/<date>/daemon.jsonl and write status JSON to ~/.gm/<daemon>-status.json. Graceful error handling on missing/stale daemons, no unhandled rejections. Works in exec:nodejs spool contexts without hook infrastructure. Enables gm-skill to manage acptoapi, rs-learn, and rs-codeinsight daemons independently of plugkit. Updated gm-skill/index.js to import from lib/ (not gm-starter/); updated package.json to export "./daemon-bootstrap": "./lib/daemon-bootstrap.js" for direct import paths.

## 2026-05-14 - gm-skill: automated npm publish with version bump

Created `.github/workflows/gm-skill-publish.yml` to automatically publish gm-skill to npm on every push to main. Workflow: (1) checks current gm-skill/package.json version vs npm registry; (2) auto-bumps patch version if npm published version is >= local (e.g., 0.0.0 npm -> 0.1.0 local bumps to 0.1.1); (3) commits and pushes version bump if needed with `[skip ci]` marker; (4) publishes to npm with `--access public` using NODE_AUTH_TOKEN from GitHub secrets.NPM_TOKEN; (5) on success, triggers 12-platform rebuild via `gh workflow run publish.yml` to cascade gm-skill updates to all downstream repos (gm-cc, gm-gc, gm-oc, gm-kilo, gm-codex, gm-qwen, gm-copilot-cli, gm-hermes, gm-antigravity, gm-vscode, gm-cursor, gm-zed). Conditional triggers on `paths: ['gm-skill/**', '.github/workflows/gm-skill-publish.yml']` to avoid spurious runs. Resolves user request "make sure it bumps the version and publishes on every push".

## 2026-05-14 - gm-skill: spool dispatch helpers and test coverage

gm-skill npm package now exports comprehensive spool dispatch API via `const { spool } = require('gm-skill')`. Four helpers: `writeSpool(body, lang, options)` writes code to `.gm/exec-spool/in/<lang>/` and returns task metadata; `readSpoolOutput(id)` reads completed task stdout/stderr/metadata from `.gm/exec-spool/out/`; `waitForCompletion(id, timeoutMs)` polls for task completion with configurable timeout; `getAllOutputs()` enumerates all completed tasks. Platform-aware paths (Windows/POSIX), graceful error handling on missing files, zero mocks. gm-skill/test.js exercises all four helpers with real filesystem operations over 90 lines under the 200-line budget. Enables npm consumers (freddie, downstream agents, third-party tools) to dispatch work via the gm spool without reimplementing file I/O.

## 2026-05-14 - hooks: skill-based gate logic via .gm/ markers

All 5 hooks (session_start, pre_tool_use, prompt_submit, post_tool_use, session_end) implement skill-driven gate logic reading marker files and YAML state. session_start writes `.gm/needs-gm` when `.gm/` folder exists. prompt_submit manages needs-gm based on PRD existence -- if `.gm/prd.yml` exists (autonomous work), needs-gm is written; if absent, needs-gm is deleted. pre_tool_use denies tool use if gm is needed but `.gm/gm-fired-this-turn` marker absent (lines 89-99, 140-142); on Skill(gm:gm) or Agent(subagent_type="gm:gm"), writes the marker (lines 106-127). Mutables gate (lines 178-191) reads `.gm/mutables.yml` via `scan_unresolved_mutable_ids()` and denies Write/Edit/git if any entry has `status: unknown`. prompt_submit deletes `.gm/gm-fired-this-turn` at turn start (line 46) to reset. post_tool_use tracks turn state in `.gm/turn-state.json`. session_end on real-exit cleans up tasks and browsers. Together: PRD exists -> needs-gm written -> Skill(gm:gm) required -> gm-fired marker gate clears -> mutables gate checks YAML resolution -> skill chain proceeds.

## 2026-05-14 - bootstrap: skill-bootstrap.js completed

gm-starter/lib/skill-bootstrap.js now provides shared bootstrap orchestration for all 10 output skills. Detects plugkit binary at ~/.claude/gm-tools/plugkit{.exe,}, verifies SHA256 against manifest (gm.json::plugkitVersion + bin/plugkit.sha256), downloads from AnEntrypoint/plugkit-bin Releases on mismatch, kills stale processes before re-spawn on Windows (honoring EBUSY precedent), spawns daemon in detached mode with stdio:'ignore', and emits structured JSONL events to ~/.claude/gm-log/<date>/bootstrap.jsonl. Returns Promise<{ ok, error? }> for graceful error handling in skill setup. Removed abandoned refactoring modules (plugkit-platform.js, plugkit-manifest.js) created during failed modularization attempt--skill-bootstrap.js remains single coherent concern.

## 2026-05-14 - platform skill manifests: AgentSkills.io compliance

gm-starter/skills/gm/SKILL.md + 9 platform-specific manifests (gm-cc through gm-jetbrains, excluding gm-gc per design) ship with AgentSkills.io frontmatter: name, description, allowed-tools=[Skill]. TemplateBuilder.loadSkillsFromSource emits manifests to build/gm-<platform>/skills/<name>/SKILL.md during build. No skill duplication across platforms; each platform gets its own directory. Manifests enable downstream tooling (claude.ai Routing, agent discovery, skill marketplaces) to introspect available skills without parsing internal prose.

## 2026-05-12 - bootstrap: kill-before-rename on busy gm-tools targets

Fixed silent stale-binary loop where `gm-starter/bin/bootstrap.js::copyToGmTools` swallowed `EBUSY`/`EPERM`/`EEXIST` on `renameSync(plugkit.exe.new -> plugkit.exe)` and trusted an in-Rust self-update that the same hold-open also blocked. Result: bootstrap returned ok, but `plugkit.js::isReady()` sha-checked the stale `plugkit.exe`, mismatched the manifest, and emitted `[plugkit] bootstrap failed; aborting hook` on every prompt-submit. Orphan `plugkit.0.1.*.new` files accumulated across days.

`copyToGmTools` now (a) sweeps stale `*.new` orphans before writing, (b) retries the atomic rename up to 8 times with 200 ms backoff, (c) at the halfway attempt enumerates Windows processes whose `ExecutablePath` is the target via `wmic` and terminates them with `taskkill /F`, (d) throws on exhausted retries instead of swallowing. The top-level `.catch` now writes `.gm/exec-spool/.bootstrap-error.json` alongside the JSONL `fatal` event so the documented spool sentinel surfaces this failure mode. Generalises the AGENTS.md "kill before rmSync" rule to in-place overwrite-rename of binaries held open by concurrent plugkit invocations.

## 2026-05-10 - verb-spool migration: Bash retains only git + utility verbs

Code execution (nodejs, python, bash, typescript, go, rust, c, cpp, java, deno) routes through the file-spool exclusively. The Bash tool accepts git commands and utility verbs (`exec:recall`, `exec:codesearch`, `exec:memorize`, `exec:wait`, `exec:sleep`, `exec:browser`, `exec:runner`, `exec:type`, `exec:kill-port`, `exec:forget`, `exec:feedback`, `exec:learn-status`, `exec:learn-debug`, `exec:learn-build`, `exec:discipline`, `exec:pause`, `exec:status`, `exec:close`); everything else is a spool write to `.gm/exec-spool/in/<lang>/<N>.<ext>` and the watcher emits `out/<N>.json` as systemMessage.

Updated: `gm-starter/prompts/bash-deny.txt` (canonical denial text), `gm-starter/skills/{gm,gm-execute,gm-emit,gm-complete,update-docs,planning}/SKILL.md` (replaced literal `exec:nodejs`/`exec:bash` code blocks with spool-form, kept utility-verb examples). Companion changes in rs-plugkit (BASH_DENY_MSG sync, memorize gate threshold 3->10, .gm/no-memorize-this-turn write always passes, mutables.yml/prd.yml writes excluded from counter) at rs-plugkit d8aa393, and in rs-exec (`UTILITY_LANGS` gains wait/pause/browser/feedback, ext fallback for root-level `in/<N>.<ext>` files) at rs-exec 1238e35.

## 2026-04-30 - memorize agent: scope guard against out-of-reach AGENTS.md edits

`gm-starter/agents/memorize.md` now opens with a STEP 0 reach check. Before reading or editing `<cwd>/AGENTS.md` (Step 3) or running the AGENTS.md <-> rs-learn migration audit (Step 4), the agent runs `gh api repos/<owner>/<repo> --jq .permissions.push` against the cwd's `git remote get-url origin`. If the answer is anything other than literal `true` (out-of-reach), the agent skips Step 3 and Step 4 entirely. rs-learn ingest (Step 2) still runs unconditionally -- it's a per-user store, safe regardless of repo ownership.

Why: agents running in a cwd that points at a third-party repo (e.g. `nousresearch/hermes-agent` for a downstream port) were writing project-specific porting notes into the upstream project's AGENTS.md. The upstream maintainers do not want those notes; the user has no push rights to land them; the changes piled up as local-only drift. Reach check is the same authoritative gate that rs-plugkit's stop hook now uses for push-pressure (`fix(stop-hook): skip push-pressure on out-of-reach remotes`, rs-plugkit 72a53da).

Recall key: `feedback/memorize-scope-guard`.

## 2026-04-30 - Maximal Cover: self-authorize in-spirit residuals

Maximal Cover rule (AGENTS.md + planning SKILL.md + gm SKILL.md) loosened: residuals the agent judges within the spirit of the original ask AND completable from this session are self-authorized -- agent expands the PRD and executes without re-asking. Only residuals genuinely outside the original ask OR genuinely unreachable are name-and-stop. When expanding under self-authorization, the agent declares its judgment in-response ("treating X as in-scope because Y") so the user can correct mid-chain. Silent expansion without the declaration is the failure mode this rule guards against.

Trades a one-message round-trip per discovered subset for the agent's honest read of intent. The transparency clause keeps misjudgments correctable in the same conversation. Propagates to all 12 downstream repos via `gm publish.yml` cascade. Recall key: `project/maximal-cover-self-auth`.

## 2026-04-30 - hook-spec lifted into per-repo hooks/hooks.spec.json

Each generated CLI repo (gm-cc, gm-gc, gm-codex, gm-oc, gm-kilo, gm-qwen, gm-copilot-cli) now ships its own `hooks/hooks.spec.json` next to the rendered `hooks.json`. The spec is the canonical configuration; hooks.json is the platform-targeted render that buildHooksJson(spec) produces. Downstream tooling can read the spec to introspect this repo's plugkit setup without parsing platform-specific hooks.json shapes.

Spec format: `{schemaVersion: 1, description, envVar, plugkitInvoker?, events: [{eventKey, wrapMode?, commands: [{kind:'plugkit'|'js', subcommand|file, timeout, subcommandRename?}]}]}`. Renamed every platform's `buildHooksMap()` override to `buildHookSpec()` returning the spec object directly. cli-adapter.generateHooksJson runs buildHooksJson(spec).hooks for the rendered output and emits the spec verbatim as hooks.spec.json.

test.js now asserts spec presence and roundtrip equality (spec -> buildHooksJson -> must match hooks.json bytes) for all 7 platforms; future drift between spec and rendered output fails CI.

## 2026-04-30 - hook-spec descriptor unification

7 platforms (cc, gc, codex, oc, kilo, qwen, copilot-cli) had drifted into 7 hand-written `buildHooksMap()` overrides spread across `platforms/cli-config-shared.js` and `platforms/copilot-cli.js`, plus a coexisting `TemplateBuilder.buildHooksMap` and `lib/hook-builder.js::buildHooksMapCustom` that were no longer the source of any output. Adding a platform required learning every dimension by reading the existing examples.

Collapsed into one `lib/hook-spec.js::buildHooksJson(spec)` (51L). Each platform now declares `{envVar, plugkitInvoker: 'node'|'binary', wrapMode: 'wrapped'|'flat-matchers', events: [{eventKey, commands: [{kind:'plugkit'|'js', subcommand|file, timeout, subcommandRename?, wrapMode?}]}]}`. Substitutes env-var placeholders, picks plugkit prefix shape (cc/codex/oc/kilo/qwen use `node ${env}/bin/plugkit.js hook`, copilot uses bare-binary `${env}/bin/plugkit hook`), and emits the matcher wrapper. qwen's per-command flat-matcher quirk and copilot's stop->session-end / stop-git->session-end-git renames are encoded as data, not code.

All 7 generated `hooks.json` byte-identical to baseline. Deleted: `lib/hook-builder.js`, `TemplateBuilder.buildHooksMap`, `TemplateBuilder.createCommand`, `TemplateBuilder.buildHookCommandWithEnv`, `CLIAdapter.buildHookCommand`, `CLIAdapter.createCustomCommand`. New platform = one `buildHooksMap()` override returning `buildHooksJson(spec).hooks`.

## 2026-04-24 - ccsniff compliance audit + stop-hook isMeta fix

ccsniff audit across 6 sessions (rs-learn, agentgui, guru-gcs, thebird, diagen, voice):

**Root causes identified:**
- Dominant violation (87% of non-compliant turns): stop-hook feedback messages have isMeta:true, bypassing UserPromptSubmit hook. Pre-tool-use sentinel never written -> model responds with Bash directly.
- Secondary (8%): Skill(gm:planning) called directly instead of Skill(gm) -- already blocked by pre-tool-use (only gm/gm:gm clears needs-gm).
- Minor (5%): compaction/session-continued messages similarly bypass hook.

**Fix (rs-plugkit):** write_needs_gm() called before every block decision in run_stop(), run_stop_git(), and run_stop(). Also write_needs_gm() in pre_compact run(). Pre-tool-use hook then enforces gm-first even for isMeta-triggered responses.

**True compliance rate (excluding isMeta/hook/compact):** ~80-85% across sessions.

## 2026-04-24 - PostToolUse hook + stronger memorize/narration enforcement (ccsniff-driven)

ccsniff audit (7d, gm sessions): 145 narrate-before-tool violations, 41/74 turns starting with text (not tool), 0 actual memorize Agent spawns despite 51 text mentions. Fixes:

- `gm-starter/hooks/post-tool-use-hook.js`: rewritten -- stdin fd 0 fix, memorize reminder injected after every Bash exec: completion
- `gm-starter/hooks/hooks.json`: added PostToolUse entry wiring post-tool-use-hook.js
- `platforms/cli-config-shared.js`: added `PostToolUse` to CC `buildHooksMap()`, added `createCcPostToolUseHook()`, wired into `getAdditionalFiles()`
- `rs-plugkit/src/hook/prompt_submit.rs`: memorize rule strengthened -- exact invocation syntax, parallel spawn mandate, explicit anti-patterns ("saying you will memorize != memorizing")
- `rs-plugkit/src/hook/pre_compact.rs`: added memorize self-check section to post-compact reminder

## 2026-04-24 - rs-plugkit prompt_submit upgrade: systemMessage + memorize/no-narration enforcement

- `rs-plugkit/src/hook/prompt_submit.rs`: switched CC output from `additionalContext` to `systemMessage` -- injected into system prompt (stronger) instead of system reminder (weaker)
- Added memorize-on-resolution rule to prompt_submit message: explicit trigger contract, `Agent(subagent_type='gm:memorize')` invocation pattern, end-of-turn self-check mandate; directly targets `missing-memorize-on-unknown` as dominant ccsniff violation (57% compliance -> fix)
- Added no-narration-before-execution rule: blocks describe-then-do pattern; directly targets `narrative-before-execution` as secondary ccsniff violation
- CI cascade: rs-plugkit push -> binary rebuild -> gm publish -> all 12 downstream plugins updated automatically

## 2026-04-24 - gm-first enforcement + state machine phase guards

- New `hooks/prompt-submit-hook.js` (CC): writes `.gm/needs-gm` sentinel on every prompt; injects systemMessage demanding gm invocation and subagent parallelism as the first action.
- Updated `hooks/pre-tool-use-hook.js` (CC + gm-starter): blocks any non-Skill tool if `.gm/needs-gm` exists; clears sentinel when `Skill(gm)` is invoked; blocks `Write`/`Edit`/`NotebookEdit` in `gm-complete` and `update-docs` phases (prevents file mutations after emission).
- CC `buildHooksMap` override in `platforms/cli-config-shared.js`: generates hooks.json with plugkit binary + JS hook for both `UserPromptSubmit` and `PreToolUse`; fixes `Stop` timeouts (stop=15s, stop-git=180s -- was inverted 300s/60s causing 3m20s session-end hang).
- ccsniff 48h analysis: 57% compliance across 23,746 messages; dominant violation = `missing-memorize-on-unknown` (agent finds/fixes facts but doesn't spawn memorize in same turn); secondary = `narrative-before-execution` (agent describes intent before running tool). Both are now actively enforced via hooks.

## 2026-04-23 - ccsniff + gm skillset alignment

- New `ccsniff-gm-lint.js`: optional linting module that analyzes conversation transcripts against gm tier-0 rules. Detects missing-skill-invocation, missing-memorize-on-unknown, bash-direct violations, narrative-before-execution. Produces JSON report for compliance auditing. Tested against 54 recent assistant messages: 98% compliance with gm skillset (49/50 conformant, 1 missing-skill edge case). Tool ready for integration into pre-commit hooks and agent training workflows.
- Updated AGENTS.md with ccsniff compliance audit findings.

## 2026-04-22 - Paper III published

- New `docs/paper3.html` -- "Governance, Earned Emission, and the Remote-Execution Boundary" -- covers 197 commits since paper II (2026-04-12): the governance layer (7 route families, 4 state planes, 16-mode taxonomy, DeltaS/lambda/epsilon/Coverage metrics, 8-case stress suite, 5 refused collapses); `git push` as remote-execution event with mandatory CI watch; catalogue of 4+1 parse-time CI failure modes (block-scalar heredoc indentation, missing token env, concurrent-push races, gitignored outputs, multiline output expansion); same-turn memorize discipline with end-of-turn self-check.
- Added paper3 nav links across paper.html, paper2.html, made-with.html, stats.html, site/main.js.

## 2026-04-22 - Governance layer in skill chain

- New `governance` skill: encodes route discovery (7 route families), weak-prior bridge, legitimacy gate (earned specificity / lawful downgrade), 16-failure taxonomy, 4 state planes (route_fit / authorization / repair_legality / hidden_decision_posture), DeltaS/lambda/epsilon/Coverage quality metrics, and 8-case governance stress suite (M1/F1/C1/H1/S1/B1/A1/D1).
- `planning` skill: route_family tagging + failure-mode mapping required on `.prd` items with emission impact; .prd YAML schema gains `route_family`, `failure_modes`, `route_fit`, `authorization`; competing routes kept live until witnessed dominance.
- `gm-execute` skill: weak-prior bridge -- hypotheses from PLAN arrive as weak priors, never as authorization; witnessed probe required before `authorization=witnessed`. Quality metrics DeltaS=0 / lambda>=2 / epsilon intact / Coverage>=0.70 gate UNKNOWN->KNOWN transition.
- `gm-emit` skill: legitimacy gate before pre-emit -- earned specificity, repair legality, lawful downgrade preferred over forced closure, live-alternative preservation. Five refused collapses enumerated in gate conditions.
- `gm-complete` skill: `stress_suite_clear` + `hidden_decision_posture=closed` added as completion mutables; hygiene sweep step 15 walks finished change through the 8 stress cases.
- Built to all 10 platforms (gm-cc/gc/oc/codex/kilo/qwen/hermes/vscode/cursor/copilot-cli/jetbrains).

## 2026-04-18 - Localize CDN imports in platform GH Pages

- Replaced rippleui@1.12.1 CDN link (4.7MB) with Tailwind CDN play script (~100KB, processes classes on-the-fly)
- Inlined webjsx@0.0.42 bundle (16KB) directly into page template -- eliminates importmap + CDN dependency
- Platform pages now fully self-contained, no external asset fetches required for rendering

## 2026-04-18 - Fix Fragment/function-component crash in platform pages

- Fixed `Invalid element name` crash in all 12 platform GH Pages sites caused by webjsx not supporting function components passed to `h()` or `Fragment` wrapper in `App`
- `page-generator.js`: replaced destructured component calls with plain function calls, removed Fragment import, inlined `applyDiff` call with array of section results
- Added GH Pages workflow (`pages.yml`) + enabled GH Pages for 8 repos that were missing it: gm-hermes, gm-codex, gm-copilot-cli, gm-qwen, gm-vscode, gm-cursor, gm-zed, gm-jetbrains

## 2026-04-17 - PDF search across skills

- Propagated "PDFs searchable as code" capability through skills: code-search, planning, gm-execute, gm-emit, gm-complete.
- AGENTS.md: new caveat documenting rs-search PDF ingestion (pdf-extract crate, pdf-cache dir, ignore.rs changes).

## 2026-05-14 - rs-plugkit: track acptoapi process PID for session cleanup

Added PID tracking for dynamically spawned acptoapi daemon process. `session_start.rs::spawn_acptoapi_daemon()` now captures and returns the child process ID (u32) instead of discarding it. On successful spawn, the PID is written to `~/.claude/gm-tools/.acptoapi-pid`. `session_end.rs::kill_acptoapi_if_running()` reads the PID file, logs a cleanup event via `rs_exec::obs::event()`, and calls `rs_exec::kill::kill_tree()` to terminate the process tree. Cleanup runs on real-exit reasons only (clear/logout/prompt_input_exit); handoff/compaction preserves the acptoapi instance for the next session. Failure to kill (e.g., process already dead) is best-effort and does not block session cleanup.

# Changelog

## [Unreleased]
- Fix GC exec dispatch: use `plugkit exec --lang <lang> <code>` instead of positional arg (was treating lang as code)
- Deploy OC plugin (gm-oc.mjs) with all 5 handlers: session.closing, tool.execute.before, message.updated, experimental.chat.system.transform, experimental.chat.messages.transform
- Install gm-kilo plugin into ~/.kilocode: kilocode.json, gm-kilo.mjs, agents/, hooks/
- Deploy GC bin/ (plugkit.js + binaries) to ~/.gemini/extensions/gm/bin/
- Deploy updated GC hooks with exec dispatch to ~/.gemini/extensions/gm/hooks/
- Add validate/probe.js: 14-scenario parity harness for OC and GC hook scripts (14/14 passing)


### Fixed
- Align file-edit blocking rules across gc/kilo/oc to match cc exactly: add Edit tool to kilo/oc check, add edit_file to GC writeTools, add jest/vitest config and snap/stub/mock/fixture patterns to GC FORBIDDEN_FILE_RE, add /fixtures/ and /test-data/ to GC FORBIDDEN_PATH_RE
