---
key: mem-17b33aec045da8fb-791
ns: default
created: 1787557964594
updated: 1787557964594
---

gm repo Build section detail: install.sh/install.ps1 are the canonical installers for the gm-skill repo (no npx, no marketplace, no npm registry involved) -- landed directory name IS the /command name. This is verified by manually running the installer and checking the landed tree, never by an automated guard. AnEntrypoint/gm-skill (a separate mirror repo) last received a real mirror push at v2.0.2597 (2026-08-13); the mirror job needed a cross-repo token and was removed rather than reintroducing a manually-configured secret (commit 36c41c3) -- this project keeps zero manually-configured secrets. gm-skill is now a stale, unmaintained snapshot, not a live back-compat surface. Every install path (install.sh/install.ps1, bun x skills add) targets AnEntrypoint/gm only, never gm-skill.
