---
key: mem-6678de9b2997192e-1432
ns: default
created: 1787473375041
updated: 1787473375041
---

## Resolved mutable: algo3-divergence-decision

paper.txt fetched from cordiverse/paper (curl+WebSearch); Algorithm 3 text lines 2951-2966: notify(ctx,keys) iterates all_fibers, tests key in fiber.inject and same-realm resolution, calls refresh on match. Definition 26 lines 950-970: notify_d(sigma,sigma') classifies activating/deactivating/neutral by satisfaction-predicate delta. rs-plugkit discipline_note.rs:804 active_policies() is the sole call site (instructions/mod.rs:578), invoked exactly once per instruction dispatch in gm's single-threaded synchronous orchestrator -- confirmed via grep, zero other callers exist, so no concurrent mutator can change a binding between two instruction calls without itself being a dispatch that would trigger the next active_policies() call. Full re-derivation each dispatch is therefore a correctness-equivalent realization of notify's activate/deactivate classification: every state transition IS an instruction dispatch, so every transition is observed, matching Definition 26's own reactive invariant text ('every coeffect change is observed') without needing push machinery. removal_dependents() (discipline_note.rs:406-451) additionally enforces the withdrawal-ordering guard pre-emptively (Theorem 63), stronger than Algorithm 3's post-hoc notify. Decision: legitimate divergence, documented in coeffect_realm.rs doc comments and rs-plugkit/formal/CordisCalculus/Isolation.lean.
