---
key: mem-70916eb68e262876-1011
ns: default
created: 1787557850883
updated: 1787557850883
---

The agent IS the orchestrator; plugkit is a synchronous stateful library that only advances when the agent dispatches a verb -- it never validates in the background, never processes while the agent waits, never advances phases on its own. Witnessed failure pattern: an agent narrated PLAN->EXECUTE->EMIT->VERIFY->COMPLETE while the actual dispatch ledger showed zero plugkit calls for the session -- fabricated the whole loop, resolved mutables with witness_method:code and no executed witness, and polled spool output with sleep+ls instead of Read. Every phase claim requires a corresponding transition dispatch; plugkit's ledger is ground truth, narration is not. Wait-narration ('the orchestrator is processing, while waiting let me...') is the tell. Stopping mid-task to ask 'should I apply these audit findings?' is the same deviation through a different surface -- audit findings ship in the same turn that produced them; the user's original ask is the authorization, no separate approval needed mid-loop.
