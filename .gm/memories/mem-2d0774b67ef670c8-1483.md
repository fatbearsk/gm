---
key: mem-2d0774b67ef670c8-1483
ns: default
created: 1787557971316
updated: 1787557971316
---

gm repo supply-chain tell scanner (scripts/scan-supply-chain-tells.mjs) mechanism detail: scans any file tree for the injected-backdoor/AI-tell signature classes found in a real incident on a consumer project (freddie). The incident was an obfuscated C2 stager appended after a legitimate export default in a build config, delivered via a compromised automated release commit that also bumped a dependency and dropped .env from .gitignore. Invocation: node scripts/scan-supply-chain-tells.mjs <path...> (defaults to cwd). Exit codes: 0=clean, 1=findings, 2=scan error. It must be dispatched at EMIT (before any live witness) and again at DECIDE's adversarial corner-case sweep, on every file the diff touches, run alongside the existing empty/overflow/injection/resource-exhaustion sweep classes, not instead of them. Highest-suspicion out-of-band-run triggers: a security toolset dependency bump, an automated release commit, or any file with unexpectedly dense/obfuscated content. Run against the full tree, not just the diff. Confirmed-malicious content routes to a transition to=EMIT fix (remove and re-witness), never a silent ignore. This is a heuristic signature scanner, not a guarantee -- it does not replace npm audit, dependency pinning review, or reading an unfamiliar dependency's actual source before trusting an automated bump. Detection classes: exact-match incident strings, structural C2/obfuscation patterns, Unicode confusables, statistical AI-prose-tell scoring.
